name: OPA Gatekeeper

on:
  push:
    branches:
      - test

jobs1:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Start minikube
        id: minikube
        uses: medyagh/setup-minikube@master

      - name: Try the cluster !
        run: kubectl get pods -A 



     


      
          

      





